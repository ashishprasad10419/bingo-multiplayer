server:
  port: ${PORT:8080}

spring:
  application:
    name: bingo-backend
  data:
    mongodb:
      database: ${MONGO_DATABASE:bingo_db}
      uri: ${MONGODB_URI:mongodb+srv://ashishprasad10419_db_user:dvuJFI6fSN8vGWvg@cluster0.uujxfcc.mongodb.net/bingo_db?retryWrites=true&w=majority&appName=Cluster0}
      auto-index-creation: true

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration-ms: 86400000 # 24 hours
  cookie-name: token

cors:
  allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173,https://bingoplay.vercel.app,https://*.ngrok-free.app,https://*.ngrok.app,https://*.vercel.app}

game:
  board-size: 5
  winning-lines: 5
  max-players: 6
  disconnect-grace-seconds: 60

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
